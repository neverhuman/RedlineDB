# Portable target defaults and the system linker; no host-only prerequisites.
# Local tuning: RUSTFLAGS='-C target-cpu=native' cargo build --release.
# Set CARGO_BUILD_JOBS to limit concurrency on smaller machines.
