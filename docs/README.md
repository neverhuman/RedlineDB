# RedlineDB documentation

Start with:

- `architecture.md` for component and authority boundaries;
- `boundaries.md` for fail-closed storage and release invariants;
- `testing.md` for local and protected proof lanes;
- `release.md` for candidate, integrity, and rollback controls;
- `exceptions/README.md` for reviewed native exceptions.

The local Jeryu forge and root-sealed `redline-core/required` check are the
release authority. Hosted workflow files are informational mirrors and do not
authorize publication or promotion.
