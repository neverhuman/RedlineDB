# Jain 8.0.0 release evidence

Receipts in this directory use the governed machine-readable release schema.
`not_run` means the capability was unavailable or the bounded lane was not
executed; it is never a passing result. Final GA publication must replace the
`PENDING` lock commits and attach green receipts for every required capability.
