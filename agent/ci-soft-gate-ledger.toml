# CI soft-gate ledger.
#
# Every soft gate currently in effect MUST have a row here. Anything not
# on this ledger is, by policy, a hard gate.
#
# The workflow YAML carries NO `continue-on-error: true` (and therefore
# does not trip HLT-034-CI-BAD-BEHAVIOR or HLT-020-CI-HARDENING-GAP).
# All soft-gate semantics live inside the `ops/ci/*.sh` scripts via the
# `ci_soft_gate` helper in `ops/ci/lib.sh`. Each entry below names the
# SCRIPT path + line where the `ci_soft_gate` wrapper is invoked, so an
# auditor can grep from the ledger to the soft-gate call site without
# touching the workflow YAML.
#
# Audit references: HLT-016 supply-chain-drift, HLT-034 ci-bad-behavior,
# HLT-020 ci-hardening-gap, HLT-042 ci-local-parity.
#
# Schema (per [[entry]] table):
#   name              — short ID matching the first arg of `ci_soft_gate`
#   script_path       — repo-relative path to the script that wraps the gate
#   line_number       — line of the `ci_soft_gate` directive inside the script
#   rule_waived       — short ID for the rule the soft gate suppresses
#   unblock_condition — concrete event that lets us flip the gate to hard
#   unblock_owner     — who owns the unblock action

[[entry]]
name              = "jankurai-install"
script_path       = ".github/workflows/jankurai.yml"
line_number       = 69
rule_waived       = "ci-bad-behavior.audit-job-nonblocking"
unblock_condition = "Confirm that `cargo install --git https://github.com/jeppsontaylor/Jankurai.git --tag v0.8.16 --package jankurai --locked` completes successfully from a GitHub Actions ubuntu-24.04 runner. When confirmed, the install step's soft-gate output check can be removed and all downstream jankurai steps made unconditional."
unblock_owner     = "jepson@veox.ai"

[[entry]]
name              = "jankurai-audit-job"
script_path       = "ops/ci/jankurai-audit.sh"
line_number       = 214
rule_waived       = "ci-bad-behavior.audit-job-nonblocking"
unblock_condition = "Local-only script; CI now uses inline steps in .github/workflows/jankurai.yml. This ledger entry covers the ops/ci/jankurai-audit.sh local proof lane path only. Unblock: same as jankurai-install entry above."
unblock_owner     = "jepson@veox.ai"

[[entry]]
name              = "jankurai-copy-code"
script_path       = "ops/ci/jankurai-audit.sh"
line_number       = 138
rule_waived       = "ci-bad-behavior.copy-code-unsupported"
unblock_condition = "Confirm that the installed jankurai binary exposes a working `copy-code` command and emits the copy-code evidence artifacts without a soft gate. When confirmed, remove the soft-gate wrapper and the placeholder artifact fallback."
unblock_owner     = "jepson@veox.ai"

[[entry]]
name              = "jankurai-ux-audit"
script_path       = "ops/ci/jankurai-audit.sh"
line_number       = 161
rule_waived       = "ci-bad-behavior.ux-audit-artifact-missing"
unblock_condition = "Restore or build the UX QA CLI artifact expected by `jankurai ux audit` so the command succeeds in CI without a soft gate. When confirmed, remove the placeholder artifact fallback."
unblock_owner     = "jepson@veox.ai"

[[entry]]
name              = "jankurai-language-bad-behavior"
script_path       = ".github/workflows/jankurai.yml"
line_number       = 119
rule_waived       = "ci-bad-behavior.language-tests-clone-optional"
unblock_condition = "Confirm that `git clone --depth 1 https://github.com/jeppsontaylor/Jankurai.git` completes from a GitHub Actions runner. When confirmed, remove the clone-failed soft-gate branch."
unblock_owner     = "jepson@veox.ai"

[[entry]]
name              = "dependency-review-action"
script_path       = "ops/ci/dependency-review.sh"
line_number       = 41
rule_waived       = "supply-chain-drift.dependency-review-nonblocking"
unblock_condition = "GitHub Advanced Security `Dependency graph` is enabled at the repo level (Settings -> Security -> Code security and analysis -> Dependency graph). When enabled, drop the `ci_soft_gate` wrapper inside ops/ci/dependency-review.sh and let cargo-deny's exit propagate; also drop the wrapper from the workflow step in `.github/workflows/jankurai.yml`."
unblock_owner     = "jepson@veox.ai"
