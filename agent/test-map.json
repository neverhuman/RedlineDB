{
  "tests": {
    ".bundles/": {
      "command": "cd .bundles && sha256sum --check *.bundle.sha256 && git bundle verify *.bundle",
      "lane": "release",
      "purpose": "verify preserved all-ref custody bundles and their checksums"
    },
    ".bundles/**": {
      "command": "cd .bundles && sha256sum --check *.bundle.sha256 && git bundle verify *.bundle",
      "lane": "release",
      "purpose": "verify preserved all-ref custody bundles and their checksums"
    },
    ".cargo/": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify portable cargo configuration remains parseable"
    },
    ".cargo/**": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify portable cargo configuration remains parseable"
    },
    ".ci-status/": {
      "command": "just required",
      "lane": "required",
      "purpose": "verify generated local CI status evidence is regenerated by the required lane"
    },
    ".ci-status/**": {
      "command": "just required",
      "lane": "required",
      "purpose": "verify generated local CI status evidence is regenerated by the required lane"
    },
    ".config/": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify workspace metadata remains parseable"
    },
    ".config/**": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify workspace metadata remains parseable"
    },
    ".dockerignore": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify workspace metadata remains parseable"
    },
    ".gitattributes": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify Git/LFS attributes stay routed"
    },
    ".github/": {
      "command": "just check && just tool-adoption",
      "lane": "ci",
      "purpose": "verify workflows remain pinned and adoption evidence is wired"
    },
    ".github/**": {
      "command": "just check && just tool-adoption",
      "lane": "ci",
      "purpose": "verify workflows remain pinned and adoption evidence is wired"
    },
    ".gitignore": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify workspace metadata remains parseable"
    },
    ".jankurai/": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify committed audit evidence matches the pinned Jankurai lane"
    },
    ".jankurai/**": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify committed audit evidence matches the pinned Jankurai lane"
    },
    "AGENTS.md": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify split metadata and agent maps"
    },
    "CHANGELOG.md": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify release notes stay routed through the score lane"
    },
    "Cargo.lock": {
      "command": "just required",
      "lane": "required",
      "purpose": "verify the split lockfile resolves"
    },
    "Cargo.toml": {
      "command": "just required",
      "lane": "required",
      "purpose": "verify workspace manifest shape and proof lane"
    },
    "Justfile": {
      "command": "just fast && just check && just required",
      "lane": "ci",
      "purpose": "verify local command wrappers remain canonical"
    },
    "LICENSE": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify workspace metadata remains parseable"
    },
    "README.md": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify root navigation remains current"
    },
    "RELEASE_PROCESS.md": {
      "command": "just check && just score",
      "lane": "release",
      "purpose": "verify release process evidence remains aligned with the audit lane"
    },
    "ROLLBACK.md": {
      "command": "just check && just score",
      "lane": "release",
      "purpose": "verify rollback guidance remains aligned with immutable tag policy"
    },
    "SPLIT.md": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify split provenance and inherited source cap policy"
    },
    "VERSION": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify version source remains aligned with release evidence"
    },
    "agent/": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify agent metadata parses and routes proof lanes"
    },
    "agent/**": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify agent metadata parses and routes proof lanes"
    },
    "authority/": {
      "command": "cargo run --locked --quiet -- source-coverage --manifest repos.manifest.toml",
      "lane": "required",
      "purpose": "verify the immutable source inventory remains normalized, count-bound, and digest-bound"
    },
    "authority/**": {
      "command": "cargo run --locked --quiet -- source-coverage --manifest repos.manifest.toml",
      "lane": "required",
      "purpose": "verify the immutable source inventory remains normalized, count-bound, and digest-bound"
    },
    "contracts/": {
      "command": "cargo test --locked published_contract && just contract-drift",
      "lane": "contract",
      "purpose": "verify release-candidate schemas remain synchronized with their closed Rust runtime contracts"
    },
    "contracts/**": {
      "command": "cargo test --locked published_contract && just contract-drift",
      "lane": "contract",
      "purpose": "verify release-candidate schemas remain synchronized with their closed Rust runtime contracts"
    },
    "deny.toml": {
      "command": "just security",
      "lane": "security",
      "purpose": "verify security policy metadata remains parseable"
    },
    "db/": {
      "command": "just check && just score",
      "lane": "db",
      "purpose": "verify durable-state policy docs remain routed and auditable"
    },
    "db/**": {
      "command": "just check && just score",
      "lane": "db",
      "purpose": "verify durable-state policy docs remain routed and auditable"
    },
    "docs/": {
      "command": "just check && just score",
      "lane": "docs",
      "purpose": "verify agent-readable docs remain routed and auditable"
    },
    "docs/**": {
      "command": "just check && just score",
      "lane": "docs",
      "purpose": "verify agent-readable docs remain routed and auditable"
    },
    "family.lock": {
      "command": "just required",
      "lane": "required",
      "purpose": "verify family lock parses and records split pins"
    },
    "hooks/": {
      "command": "bash -n hooks/pre-push",
      "lane": "check",
      "purpose": "verify repository-local Git hooks remain valid shell"
    },
    "hooks/**": {
      "command": "bash -n hooks/pre-push",
      "lane": "check",
      "purpose": "verify repository-local Git hooks remain valid shell"
    },
    "infrastructure.lock.toml": {
      "command": "just required",
      "lane": "required",
      "purpose": "verify required infrastructure remains manifest-bound"
    },
    "ops/": {
      "command": "just check && just tool-adoption",
      "lane": "ci",
      "purpose": "verify local CI wrappers are reproducible and audit-visible"
    },
    "ops/**": {
      "command": "just check && just tool-adoption",
      "lane": "ci",
      "purpose": "verify local CI wrappers are reproducible and audit-visible"
    },
    "ops/AGENTS.md": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify workspace metadata remains parseable"
    },
    "ops/ci/": {
      "command": "just check && just tool-adoption",
      "lane": "ci",
      "purpose": "verify local CI wrappers are reproducible and audit-visible"
    },
    "ops/ci/**": {
      "command": "just check && just tool-adoption",
      "lane": "ci",
      "purpose": "verify local CI wrappers are reproducible and audit-visible"
    },
    "ops/ci/artifact_support.sh": {
      "command": "just artifact-support",
      "lane": "artifact-support",
      "purpose": "verify artifact-support evidence generation remains runnable"
    },
    "ops/dev/": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify workspace metadata remains parseable"
    },
    "ops/dev/**": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify workspace metadata remains parseable"
    },
    "ops/git-hooks/": {
      "command": "just check",
      "lane": "ci",
      "purpose": "verify pre-push gates delegate to local CI lanes"
    },
    "ops/git-hooks/**": {
      "command": "just check",
      "lane": "ci",
      "purpose": "verify pre-push gates delegate to local CI lanes"
    },
    "ops/split/": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify split helper scripts parse and stay manifest-driven"
    },
    "ops/split/**": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify split helper scripts parse and stay manifest-driven"
    },
    "repos.manifest.toml": {
      "command": "just check && just score",
      "lane": "check",
      "purpose": "verify split manifest remains parseable and routed"
    },
    "rust-toolchain.toml": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify local Rust toolchain metadata remains pinned"
    },
    "scripts/": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify helper scripts parse and stay locally reproducible"
    },
    "scripts/**": {
      "command": "just check",
      "lane": "check",
      "purpose": "verify helper scripts parse and stay locally reproducible"
    },
    "schemas/": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify audit output schemas stay aligned with generated Jankurai artifacts"
    },
    "schemas/**": {
      "command": "just score",
      "lane": "score",
      "purpose": "verify audit output schemas stay aligned with generated Jankurai artifacts"
    },
    "tests/": {
      "command": "just check && just score",
      "lane": "check",
      "purpose": "verify negative proof evidence remains routed"
    },
    "tests/**": {
      "command": "just check && just score",
      "lane": "check",
      "purpose": "verify negative proof evidence remains routed"
    },
    "tools/": {
      "command": "just required",
      "lane": "required",
      "purpose": "verify the Rust control plane and its safety invariants"
    },
    "tools/**": {
      "command": "just required",
      "lane": "required",
      "purpose": "verify the Rust control plane and its safety invariants"
    }
  },
  "workspace": "jain-split-ops"
}
