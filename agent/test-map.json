{
  "workspace": "redline-core",
  "tests": {
    ".agents/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify agent context through the canonical host-required lane"},
    ".cargo/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify workspace configuration through the canonical host-required lane"},
    ".cursor/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify editor context through the canonical host-required lane"},
    ".editorconfig": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify editor configuration through the canonical host-required lane"},
    ".github/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify forge automation through the canonical host-required lane"},
    ".gitignore": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify ignored evidence boundaries through the canonical host-required lane"},
    ".gitlab-ci.yml": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify CI compatibility through the canonical host-required lane"},
    ".jankurai/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify proof policy through the canonical host-required lane"},
    ".jeryu/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify Jeryu policy through the canonical host-required lane"},
    "AGENTS.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify agent instructions through the canonical host-required lane"},
    "AGENT_CHAT.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify agent coordination through the canonical host-required lane"},
    "CHANGELOG.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify release notes through the canonical host-required lane"},
    "CLAUDE.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify agent instructions through the canonical host-required lane"},
    "CONTRIBUTING.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify contributor guidance through the canonical host-required lane"},
    "Cargo.lock": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify the locked dependency graph through the canonical host-required lane"},
    "Cargo.toml": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify workspace metadata through the canonical host-required lane"},
    "EXTERNAL_REVIEW_MASTER_PROMPT.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify review instructions through the canonical host-required lane"},
    "EXTERNAL_REVIEW_SUPERPROMPT.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify review instructions through the canonical host-required lane"},
    "FEATURE_GAPS.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify feature-gap documentation through the canonical host-required lane"},
    "GEMINI.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify agent instructions through the canonical host-required lane"},
    "LICENSE": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify licensing through the canonical host-required lane"},
    "README-jankurai-scaffold.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify proof documentation through the canonical host-required lane"},
    "README.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify product documentation through the canonical host-required lane"},
    "SECURITY.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify security guidance through the canonical host-required lane"},
    "agent/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify proof routing through the canonical host-required lane"},
    "apps/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify application code through the canonical host-required lane"},
    "assets/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify generated and published assets through the canonical host-required lane"},
    "benchmark-results/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify benchmark evidence through the canonical host-required lane"},
    "branch_recovery_ledger.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify recovery records through the canonical host-required lane"},
    "contracts/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify public contracts through the canonical host-required lane"},
    "crates/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify Rust product code through the canonical host-required lane"},
    "db/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify database fixtures through the canonical host-required lane"},
    "deny.toml": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify dependency policy through the canonical host-required lane"},
    "docs/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify documentation through the canonical host-required lane"},
    "install.sh": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify installation automation through the canonical host-required lane"},
    "just/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify task automation through the canonical host-required lane"},
    "justfile": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify task automation through the canonical host-required lane"},
    "metadata/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify release metadata through the canonical host-required lane"},
    "ops/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify operational automation through the canonical host-required lane"},
    "paper/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify technical reports through the canonical host-required lane"},
    "reference/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify reference fixtures through the canonical host-required lane"},
    "rust-toolchain.toml": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify the pinned toolchain through the canonical host-required lane"},
    "rustfmt.toml": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify formatting policy through the canonical host-required lane"},
    "scripts/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify repository automation through the canonical host-required lane"},
    "speed_up_workplan_FINAL.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify workplan records through the canonical host-required lane"},
    "speed_up_workplan_pending.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify workplan records through the canonical host-required lane"},
    "super_tasks.md": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify task records through the canonical host-required lane"},
    "tips/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify archived guidance through the canonical host-required lane"},
    "tools/": {"command": "rtk bash scripts/ci-local.sh required", "purpose": "verify repository tooling through the canonical host-required lane"}
  }
}
