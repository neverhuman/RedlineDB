\section{Introduction}\label{sec:intro}

SQLite is the most widely deployed database engine on
Earth~\cite{sqlite_mostdeployed}: every major mobile platform, every
mainstream web browser, the in-flight entertainment system on commercial
aircraft, and a long tail of embedded controllers ship a copy. Its
durability story is conservative and well-documented~\cite{sqlite_arch},
its on-disk format has been stable since 2004, and its test
discipline---roughly 92.6 million lines of test code against the
amalgamation~\cite{sqlite_testing}---is unmatched among open-source
relational engines.

The cost of that conservatism is concentrated in one place: the
write-ahead log. SQLite's WAL is a single-writer
log~\cite{sqlite_wal}; any number of readers may proceed concurrently
against the most recent committed snapshot, but at most one writer may
hold the database lock at a time. On modern many-core hosts this
serializes every write-bearing transaction onto a single critical
section regardless of whether two transactions touch disjoint rows.
Workloads that mix even a small fraction of writes flatten well below
the host's available parallelism. The architecture of the engine---a
hand-rolled five-class type affinity system, a bespoke 250-KLOC C
amalgamation~\cite{sqlite_footprint}, a virtual-machine bytecode whose
opcode set has accreted for two decades---also makes incremental
modernization difficult: any patch that touches the WAL contract risks
an ecosystem-scale regression.

We argue these two cliffs (concurrency ceiling and engineering surface)
can be removed without giving up the SQLite \emph{contract}---the C
API, the SQL dialect, the file-per-database mental model, the embedded
single-process deployment. RedlineDB is a Rust-native, MVCC,
concurrent-write embedded SQL engine that is API-compatible with
SQLite. The kernel, parser, planner, executor, public Rust facade, and
benchmark harness together total approximately 35 KLOC of safe Rust
active source (Table~\ref{tab:loc}); only the FFI shim contains
\texttt{unsafe} blocks, and those exist because the C ABI requires
them. The kernel achieves ARIES-style recovery~\cite{mohan1992aries} on
top of a slotted-heap MVCC store, and the index layer follows the
B-link discipline~\cite{lehman1981blink,srinivasan1991bplus} so that
concurrent writers on disjoint key ranges do not block each other.

\textbf{Contributions.}
\begin{enumerate}
\item A safe-Rust MVCC kernel with B-link-style concurrent index
lifecycle, group-commit WAL, and ARIES-style redo/undo
recovery---packaged as an embedded library with the SQLite mental model
intact.
\item A SQLite-compatible C ABI shim path
(\texttt{sqlite3.h}~$\cup$~\texttt{rldb\_*}) that allows existing
SQLite clients to link against RedlineDB without source changes for the
covered surface, including phase-10 SAVEPOINT and multi-statement
\texttt{prepare\_v2} / \texttt{exec} with proper \texttt{pzTail}
handling.
\item A deterministic crash certification protocol: 16 failpoint sites
exercised under a fsynced-ack child-oracle harness, observing 0 lost
acked commits across 24 scenarios.
\item A reproducible bin-packed parallel benchmark harness with
real-warmup discard, recursive on-disk footprint accounting, and
strace-passthrough telemetry, packaged into a single
manifest-stamped certification run.
\item An empirical scaling profile: 7.9--8.3$\times$ throughput on
mixed and writers-disjoint OLTP at 64 threads against SQLite 3.x
bundled by \texttt{rusqlite}~\cite{rusqlite}, parity at 64-thread point
reads, and a transparent accounting of the two workloads where SQLite
still wins.
\item A long-range capabilities layer (the phase-10 closure):
per-entry MVCC index tags with a v1$\rightarrow$v2 migration; a
\texttt{CommitOutcome::MaybeCommitted} signal that lets the SQL
layer distinguish post-fsync ambiguity from rollback; a heap /
index / page-checksum / LSN-monotonicity integrity checker exposed
via \texttt{PRAGMA redline\_index\_check} and
\texttt{PRAGMA redline\_full\_check}; a binary JSONB format with a
compiled JSON-path bytecode plus SIMD key compare; the SQLite
JSON1 surface; a native VECTOR type backing flat SIMD scan, HNSW
(recall@10 = 0.95) and a DiskANN-style graph (recall@10 = 0.99);
a vectorized executor with selection vectors, hash aggregation,
top-K heap, and an external merge-sort with spill; and a
\texttt{DatasetChecksum} for benchmark integrity that replaces the
phase-9 \texttt{MAX(k)} / \texttt{COUNT(*)} placeholder.
\end{enumerate}

The rest of this paper is organized as follows. Section~\ref{sec:background}
discusses related work; Section~\ref{sec:arch} presents the system
architecture; Section~\ref{sec:impl} covers implementation;
Section~\ref{sec:method} describes our methodology;
Section~\ref{sec:eval} reports the measured results;
Section~\ref{sec:discussion} is the honest postmortem;
Section~\ref{sec:conclusion} concludes.
