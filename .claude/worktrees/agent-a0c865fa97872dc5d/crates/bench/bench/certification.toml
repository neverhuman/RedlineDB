out_dir = "target/bench/certification"
workloads = [
  "point-read-pk",
  "secondary-index-read",
  "secondary-index-range",
  "writers-disjoint",
  "hot-row-update",
  "mixed95-read5-write",
  "mixed80-read20-write",
  "mixed50-read50-write",
  # Lane BH P1 #7: probes the engine for `max_stable_connections`
  # via a self-managed binary search; harness `--threads` is
  # ignored for this workload (the workload owns its connection
  # pool). Surfaced in `engine_stats.max_stable_connections`.
  "connection-limit",
]
durabilities = ["strict", "normal"]
threads = [1, 2, 4, 8, 16, 32, 64, 128]
rows = 4096
seconds = 2
cache_mib = 32
