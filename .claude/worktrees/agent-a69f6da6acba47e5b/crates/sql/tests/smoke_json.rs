//! JSON-surface smoke tests (`json_*` SQL functions).
//!
//! Reserved for `json_*` smoke coverage as part of the Phase 11 Wave 0
//! split of `tests/sql_smoke.rs`. The original file did not contain any
//! `json_*` function tests, so this binary is currently empty by design;
//! it exists so future JSON-surface tests have an obvious home alongside
//! `smoke_select.rs`, `smoke_dml.rs`, and friends.
