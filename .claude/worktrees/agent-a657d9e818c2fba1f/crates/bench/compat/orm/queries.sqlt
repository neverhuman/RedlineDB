statement ok
DROP TABLE IF EXISTS orders

statement ok
DROP TABLE IF EXISTS customers

statement ok
CREATE TABLE customers(id INTEGER PRIMARY KEY, name TEXT NOT NULL)

statement ok
CREATE TABLE orders(id INTEGER PRIMARY KEY, customer_id INTEGER NOT NULL, total INTEGER NOT NULL, status TEXT NOT NULL DEFAULT 'open')

statement ok
INSERT INTO customers VALUES (1, 'Ann')

statement ok
INSERT INTO customers VALUES (2, 'Ben')

statement ok
INSERT INTO orders(id, customer_id, total, status) VALUES (100, 1, 25, 'open')

statement ok
INSERT INTO orders(id, customer_id, total, status) VALUES (101, 1, 50, 'open')

statement ok
INSERT INTO orders(id, customer_id, total, status) VALUES (102, 2, 75, 'closed')

query
SELECT c.name, o.id FROM customers c LEFT JOIN orders o ON o.customer_id = c.id ORDER BY o.id
----
Ann|100
Ann|101
Ben|102

query
SELECT id FROM orders WHERE customer_id IN (SELECT id FROM customers WHERE name = 'Ann') ORDER BY id
----
100
101

statement ok
UPDATE orders SET status = 'archived' WHERE id = 102

statement ok
DELETE FROM orders WHERE id = 101

query
SELECT id, status FROM orders ORDER BY id
----
100|open
102|archived
