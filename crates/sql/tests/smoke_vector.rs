//! Vector / HNSW / DiskANN / KNN smoke tests.
//!
//! Reserved for vector-surface smoke coverage as part of the Phase 11
//! Wave 0 split of `tests/sql_smoke.rs`. The original file did not
//! contain any vector / HNSW / DiskANN / KNN tests (those live in
//! `tests/vector_basic.rs` and the dedicated kernel/vector crates), so
//! this binary is currently empty by design; it exists so future
//! vector-surface SQL tests have an obvious home alongside the rest of
//! the `smoke_*` family.
