[package]
name = "redlinedb-server"
version = "1.0.8"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
redlinedb = { path = "../redlinedb" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3.23"
