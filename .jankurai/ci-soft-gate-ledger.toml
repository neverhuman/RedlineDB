# CI soft-gate ledger.
#
# No soft gate is currently in effect. Any future non-blocking gate would
# require an explicit reviewed row here; release policy rejects such rows.
#
# The workflow YAML carries NO `continue-on-error: true` (and therefore
# does not trip HLT-034-CI-BAD-BEHAVIOR or HLT-020-CI-HARDENING-GAP).
# Repo-owned workflows and `ops/ci/*.sh` scripts propagate every failure.
#
# Audit references: HLT-016 supply-chain-drift, HLT-034 ci-bad-behavior,
# HLT-020 ci-hardening-gap, HLT-042 ci-local-parity.
#
# No release or security gate is currently soft-gated.
