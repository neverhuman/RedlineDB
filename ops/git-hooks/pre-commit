#!/usr/bin/env bash
# Canonical pre-commit entrypoint: delegate to the governed staged-file gate.
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
exec bash "$repo_root/tools/jankurai-hooks/pre-commit" "$@"
