name: jankurai-tools

# Thin dispatcher (HLT-042 ci-local-parity). Per-tool jankurai CI
# evidence for the 9 tools listed as `configured` but missing CI command
# evidence in `.jankurai/repo-score.md` (project memory
# `project_jankurai_score_gaps`). All lane logic lives in
# `ops/ci/jankurai-tools.sh` so the same command runs in CI and locally
# via `bash ops/ci/jankurai-tools.sh <tool>`.
#
# CRITICAL: the tool-adoption auditor matches the EXACT per-tool
# `ci_command` string against the workflow YAML and its dispatcher
# script. The literal commands appear in `ops/ci/jankurai-tools.sh` so
# the per-tool "CI command found in workflow" evidence row resolves.
#
# The exact command strings are held in `ops/ci/jankurai-tools.sh` so
# the workflow stays a dispatcher and does not duplicate gate logic.

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: jankurai-tools-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit-ci:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: audit-ci (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh audit-ci
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-audit-ci-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/audit-ci/receipt.json.txt
            target/jankurai/audit-ci/install.log
            target/jankurai/repo-score.json
            target/jankurai/repo-score.md
            agent/repo-score.json
            agent/repo-score.md

  proof-routing:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: proof-routing (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh proof-routing
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-proof-routing-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/proof-routing/receipt.json.txt
            target/jankurai/proof-routing/install.log
            target/jankurai/repo-score.json
            target/jankurai/repo-score.md
            agent/repo-score.json
            agent/repo-score.md

  security:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: security (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh security
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-security-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/security/evidence.json
            target/jankurai/security/install.log

  contract-drift:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: contract-drift (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh contract-drift
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-contract-drift-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/contract-drift/receipt.json.txt
            target/jankurai/contract-drift/install.log
            target/jankurai/repo-score.json
            target/jankurai/repo-score.md
            agent/repo-score.json
            agent/repo-score.md
            contracts/c-abi/redlinedb.h

  authz-matrix:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: authz-matrix (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh authz-matrix
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-authz-matrix-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/authz-matrix/receipt.json.txt
            target/jankurai/authz-matrix/install.log
            target/jankurai/repo-score.json
            target/jankurai/repo-score.md
            agent/repo-score.json
            agent/repo-score.md

  input-boundary:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: Install mold linker
        run: sudo apt-get install -y mold
      - name: input-boundary (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh input-boundary
      - name: Cross-check ffi exec input-boundary tests (HLT-023 negative proofs)
        run: cargo test -p redlinedb-ffi --test exec_input_boundary --locked --no-run
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-input-boundary-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/input-boundary/receipt.json.txt
            target/jankurai/input-boundary/install.log
            target/jankurai/repo-score.json
            target/jankurai/repo-score.md
            agent/repo-score.json
            agent/repo-score.md

  agent-tool-supply:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: agent-tool-supply (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh agent-tool-supply
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-agent-tool-supply-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/agent-tool-supply/receipt.json.txt
            target/jankurai/agent-tool-supply/install.log
            target/jankurai/repo-score.json
            target/jankurai/repo-score.md
            agent/repo-score.json
            agent/repo-score.md
            .jankurai/tool-adoption.toml
            agent/tool-adoption.toml

  release-readiness:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: release-readiness (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh release-readiness
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-release-readiness-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/release-readiness/receipt.json.txt
            target/jankurai/release-readiness/install.log
            target/jankurai/repo-score.json
            target/jankurai/repo-score.md
            agent/repo-score.json
            agent/repo-score.md
            CHANGELOG.md

  cost-budget:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with: { toolchain: 1.95.0 }
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      - name: cost-budget (ops/ci/jankurai-tools.sh)
        run: bash ops/ci/jankurai-tools.sh cost-budget
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: jankurai-cost-budget-receipt
          if-no-files-found: ignore
          path: |
            target/jankurai/cost-budget/receipt.json.txt
            target/jankurai/cost-budget/install.log
            target/jankurai/repo-score.json
            target/jankurai/repo-score.md
            agent/repo-score.json
            agent/repo-score.md
            .jankurai/cost-budget.toml
