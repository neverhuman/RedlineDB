name: ci

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    # agent-tool-supply: reviewed=ops, runner=ubuntu-22.04, review-date=2026-05-16
    # HLT-024-AGENT-TOOL-SUPPLY-GAP: runner image pinned to ubuntu-22.04
    # (immutable major release) instead of mutable ubuntu-latest; review
    # cadence and trust review tracked in agent/tool-adoption.toml
    # (agent-tool-supply entry).
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with:
          toolchain: 1.95.0
          components: rustfmt, clippy
      - uses: swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae
      # Thin dispatcher: all fast-lane logic lives in ops/ci/fast.sh so
      # CI and local (`scripts/ci-local.sh fast`, `just fast`, the
      # ops/git-hooks/pre-push hook) run the same four commands. Audit
      # reference: HLT-038 ci.local-parity.lib-missing.
      - run: bash ops/ci/fast.sh
